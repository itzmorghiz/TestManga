import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.itzmorghiz.testmanga',
  appName: 'TestManga',
  webDir: 'build/web'
};

export default config;
